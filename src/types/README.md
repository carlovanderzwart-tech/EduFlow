# Types

Gedeelde TypeScript-types die door meer dan één module worden gebruikt.
Module-specifieke types staan in `src/modules/<module>/types/`.

Leeg in Sprint 1 — de eerste gedeelde types (bijvoorbeeld het documentatie- of
mailconcept-model) komen mee met de module die ze als eerste nodig heeft.
